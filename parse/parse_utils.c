#include "../inc/push_swap.h"

char    *str_join(char  *s1, char *s2)
{
    char    *str_joined;

    str_joined = malloc()
}
char    *join_args(char *prev, char *current, int ac)
{
    if (!prev)
    {

    }
}